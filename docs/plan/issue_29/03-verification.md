# Phase 03: Regression coverage and validation

## Goal

- Add focused regression coverage and run full repository validation.

## Scope

- Extend Playwright tests for unwrapped rapid hover, wrapped rapid hover, stale render skip instrumentation, language label changes, and empty clears.
- Run `npm run build`, `npm run verify`, and `npm run test:e2e`.
- Perform a browser smoke check if feasible.

## Non-goals

- Do not add heavyweight performance benchmarks or trace artifacts.
- Do not inspect full generated diffs.

## Affected files

- `tests/language.spec.js`
- `tests/map-wrap.spec.js`
- `docs/assets/app.js` generated by `npm run build`
- `docs/plan/issue_29/*.md`

## Implementation steps

- Add assertions for buffer swap and inactive buffer rebuild counters.
- Add deterministic stale skip coverage using debug delayed commits.
- Verify stable overlay counts after swaps in `?worldWrap=0` and default wrapped mode.
- Verify language changes still avoid path overlay churn and update labels.
- Verify clearing the map empties overlay and label buffers.

## Acceptance criteria

- New tests fail against direct replacement behavior where practical and pass with buffering.
- Existing test suite passes unchanged behavior expectations.
- Phase plan records validation outcomes.

## Validation commands

- npm run build
- npm run verify
- npm run test:e2e

## Manual smoke tests

- Browser smoke: default wrapped mode and `?worldWrap=0`, rapid hover across Brazil/Bolivia/nearby regions, watch for blank overlay frames.

## Rollback risks

- Debug-only delayed commit test hook must not affect normal URLs.

## Progress

- Completed.

## Decision log

- Use debug stats rather than visual screenshot diffing to guard atomic swaps and stale skips in automated tests.

## Outcomes / Retrospective

- Added unwrapped Playwright coverage for double-buffer previous-visible behavior and stale render skips.
- Extended wrapped hover coverage to assert active-buffer projected overlays and buffer swap counters.
- Extended language/empty-clear coverage to verify label-only buffer swaps, path overlay stability, and empty inactive buffers.
- Validation passed:
  - `npm run build`
  - targeted Playwright issue #29 tests
  - `npm run verify`
  - `npm run test:e2e` (42 passed)
- Browser smoke passed against `http://127.0.0.1:4175/?debugRenderStats=1`: Brazil and Bolivia hover transitions updated the active buffer with projected overlays, and the inactive overlay buffer was empty after swap. The temporary server was stopped after verification.
