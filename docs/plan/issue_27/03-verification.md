# Phase 03: Regression coverage and validation

## Goal

- Add regression tests and run the repository validation commands after regenerating deployment output.

## Scope

- Extend Playwright coverage for issue #27 behavior in unwrapped and wrapped modes.
- Run build, verify, and e2e validation.
- Update phase outcomes with validation results and any remaining risks.

## Non-goals

- Do not add heavyweight performance benchmarks or browser tracing artifacts.
- Do not inspect or summarize full generated diffs.

## Affected files

- `tests/language.spec.js`
- `tests/map-wrap.spec.js`
- `docs/assets/app.js` generated by `npm run build`
- `docs/plan/issue_27/*.md`

## Implementation steps

- Add a next-animation-frame helper for hover preview assertions.
- Add an unwrapped hover-border test that proves next-frame claim pill update and cache hits when returning to a nation.
- Add a wrapped hover-border test that proves projected overlays remain present and cache hits survive world wrapping.
- Run `npm run build`, `npm run verify`, and `npm run test:e2e`.
- Record validation outcomes in the plan.

## Acceptance criteria

- Tests cover cache reuse for recently visited hover nations.
- Tests cover wrapped and unwrapped map modes.
- Existing tests for language-triggered label replacement and path DOM stability continue to pass.

## Validation commands

- npm run build
- npm run verify
- npm run test:e2e

## Manual smoke tests

- Browser smoke check if time permits: hover across a Brazil/Bolivia border in default and `?worldWrap=0` modes and watch for immediate pill/tooltip and smooth overlay changes.

## Rollback risks

- Test timing around requestAnimationFrame could be flaky if it assumes same-frame DOM work; use two animation frames for stability while still guarding against the removed 80 ms timer.

## Progress

- Completed.

## Decision log

- Use debug render stats instead of timing-only assertions for cache/rebuild behavior.

## Outcomes / Retrospective

- Added unwrapped Playwright coverage for next-frame border hover preview and descriptor cache hits.
- Added wrapped Playwright coverage for projected hover claim overlays and descriptor cache hits across border transitions.
- Validation passed:
  - `npm run build`
  - targeted Playwright issue #27 tests
  - `npm run verify`
  - `npm run test:e2e` (41 passed)
- Browser smoke passed against `http://127.0.0.1:4175/?debugRenderStats=1`: hovering Amazonia showed Brazil claims and hovering Bolivia showed Bolivia claims. The temporary server was stopped after verification.
